# Standard-jit-db (Just-In-Time Database)

Run `yarn download-notion-standards` to update the `standardMapping.json` file by fetching from Notion.

It will then be used by standard-jit VSCode extension.
